# Infrastructure Reference

Reference files for the Secure Document Analyzer infrastructure.

## Files

| File | Purpose |
|------|---------|
| `iam_policy.json` | IAM permissions reference (S3, Textract, KMS) |

## Note

The primary infrastructure definition is in the root `template.yaml` (AWS SAM).
This folder contains reference-only files — no CloudFormation or SAM templates
are defined here anymore.